import {
    // create naive ui
    create,
    // component
    NButton,
    NTree,
    NLayout,
    NLayoutSider,
    NLayoutHeader,
    NLayoutContent,
    NLayoutFooter,
    NInput,
    NSelect,
    NSpace,
} from 'naive-ui'

export const naive = create({
    components: [
        NButton,
        NTree,
        NLayout,
        NLayoutHeader,
        NLayoutSider,
        NLayoutContent,
        NLayoutFooter,
        NInput,
        NSelect,
        NSpace,
    ]
})
